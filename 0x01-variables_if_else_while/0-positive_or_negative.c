#include <stdlib.h>
#include <time.h>
#include<stdio.h>
/**
*main-prints whether the number stored in the variable n is pos or neg.
*Return:0
**/


int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;


	printf(" %d", n);

if (n > 0)

}
Puts(" is positive");
}
Else if (n == 0)

}
else
{
	puts(" is negative");
}

	return (0);
}

